{
  "version": 2,
  "builds": [
    {
      "src": "next.config.js",
      "use": "@vercel/next"
    }
  ],
  "@vercel/next": {
    "buildCommand": "npm run build"
  },
  "installConfig": {
    "yarn": true
  },
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "api/$1"
    },
    {
      "src": "/(.*)",
      "dest": "/"
    }
  ],
  "env": {
    "NEXT_API": "https://localhost:0402/"
  }
}
